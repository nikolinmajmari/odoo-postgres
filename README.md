- Tag this docker image and use it on docker compose of base as postgres database 
- Open bin/bash on this container 
- Switch to odoo user using `su odoo`
- Run the upgrade on premise command using `python3`
